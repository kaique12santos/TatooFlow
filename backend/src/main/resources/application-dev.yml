spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tattooflow_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: tattoouser
    password: tattoopassword
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
